import React from 'react'
import styles from './NotificationItem.module.scss'

const NotificationItem = ({ item, className }) => {
  return <div className={className}>NotificationItem</div>
}

export default NotificationItem
